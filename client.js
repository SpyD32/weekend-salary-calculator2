let newRow = 'employee';
let total = 'totalCost';
let clear = 'clearForm'
//this will be part of the trigger function
//to add the row for new employee
let html = document.getElementById('firstNameBlock', 'lastNameBlock').innerHTML

function insertRow() {
    console.log(insertRow);
    return insertRow;
}

// A new employee row should be added to the table.